implement a rtsp video stream simulator from a panoramic image. consider you have a camera looking at the panoramic image , camera is mounted on a stepper motor which can move from -60 to +60 degree angle limit. 0 degree is the middle. so camera can move 60 degrees on the right(+60) and 60 degrees on the left(-60). user can give commands to move the camera i.e the stepper motor camera rotate accordinly and gives the respective rtsp stream. make a config.yaml from where the program can read the configurable parameters. here is protocol of motor controls to be implemented. in real-world these protocol will be transfered through serial connection but for this we need to simulate. limit switch is hit when we want to move beyond the angle limit that is 60 or -60 if we set. this way user can know the current condition. rtsp stream should be stream to a mediamtx server and should be playable via ffplay. user can send command too to control and get response message. implement client side program to control the camera motion. implement this in python and use python env - @venv3.12 and this is the reference of motor commands @motor_commands.md